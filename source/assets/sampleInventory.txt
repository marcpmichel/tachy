# A tachy inventory (Pravic): the hosts tachy manages, with their
# connection settings and variables. Generated by "tachy generate
# project"; run from this directory:
#   tachy hosts list              (or just one tag: tachy hosts list @demo)
#   tachy check all               (read-only; apply makes it so)

hosts {
    example {                               # a fictitious machine: edit me
        address = "tachy.example.com"
        tags = ["demo"]                     # select it with @demo
        # user = "root"                     # ssh login user (default: current user)
        # port = 22                         # ssh port (default: 22)
        # vars { greeting = "hello" }       # host variables, templated as {{ greeting }}
    }
}
