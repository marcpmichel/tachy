---
name: behavior
description: Working style for tachy changes — ask the operator when a request is unclear, and never edit source with Python; prefer D scripts (rdmd or a shebang) or classic unix tools (sed, awk, vi).
---

# Behavior

- if something is not clear enough, ask the human operator
- do not write python code to make changes to the source code : prefer D scripts (run them with rdmd or a shebang line) or classic unix tools (sed, awk, vi).
